services:
  web:
    image: php:8.2-apache
    container_name: login-test-web
    # ports:
    #   - "8082:80"          # Keep commented if using NPM
    volumes:
      - ./html:/var/www/html
    networks:
      - panel_panel_net

networks:
  panel_panel_net:
    external: true
